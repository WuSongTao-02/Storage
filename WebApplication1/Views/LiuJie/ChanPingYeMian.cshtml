
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ChanPingYeMian</title>
    <link href="~/X-admin/css/font.css" rel="stylesheet" />
    <link href="~/X-admin/css/xadmin.css" rel="stylesheet" />
    <script src="~/X-admin/lib/layui/layui.js"></script>
    <script src="~/X-admin/js/xadmin.js"></script>
</head>
<body>
    <div class="x-nav">
        <span class="layui-breadcrumb">
            <a href="">首页</a>
            <a href="">演示</a>
            <a>
                <cite>导航元素</cite>
            </a>
        </span>
    </div>
    <div class="layui-fluid">
        <div class="layui-row layui-col-space15">
            <div class="layui-col-md12">
                <div class="layui-card">
                    <div class="layui-card-body ">
                        <div class="layui-collapse" lay-filter="test">
                            <div class="layui-colla-item">

                            </div>
                        </div>
                    </div>
                    <div>
                        <p style="margin-left:200px"><span style="font-size:18px;font-weight:bold">产品编号</span>&nbsp;&nbsp;<input type="text" id="id" style="padding:2px" />&nbsp;&nbsp;<input type="button" value="搜索" id="sosuo" style="padding:2px" /></p>
                    </div>
                    <br />
                    <div class="layui-card-header">
                        <input type="button" id="PiLiangShanChu" value="批量删除" class="layui-btn" />
                        <a href="/LiuJie/ChanPingYeMianAdd" class="layui-btn">添加</a>
                    </div>
                    <br />
                    <div class="layui-card-body ">
                        <table border="1" width="100%" class="layui-table">
                            <thead>
                                <tr>
                                    <td><input type="checkbox" name=""></td>
                                    <td>产品编号</td>
                                    <td>产品名称</td>
                                    <td>产品价格</td>
                                    <td>产品规格</td>
                                    <td>产品类别</td>
                                    <td>单位</td>
                                    <td>创建时间</td>
                                    <td>备注</td>
                                    <td>操作</td>
                                </tr>
                            </thead>
                            <tbody id="tbl">
                            </tbody>
                        </table>
                    </div>
                    <!--定义分页的容器-->
                    <div id="DemoPageList">

                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<script src="~/X-admin/lib/layui/layui.js"></script>
<script>
    var pageIndex = 1;
    var pageSize = 3;
    var pageCount = 0;
    var rows = 0;

    $(function () {
        GetRows();
        PageList(pageIndex, pageSize);
        
    });

    //分页数据查询
    function PageList(pageIndex, pageSize) {
        $.ajax({
            type: "POST",
            url: "/LiuJie/GetPageListProbaict",
            data: "{pageIndex:" + pageIndex + ",pageSize:" + pageSize + "}",
            dataType: "json",
            contentType: "application/json",
            success: function (result) {
                pageCount = result.pageCount;
                $("#tbl tr").remove();
                $.each(result.DataList, function (index, data) {
                    $("#tbl").append("<tr><td><input  type='checkbox'name='subcheck'/></td><td>" + data.ProId + "</td><td>" + data.ProName + "</td><td>" + data.ProPrice + "</td><td>" + data.PorGuiGe + "</td><td>" + data.ProCId + "</td><td>" + data.UnId + "</td><td>" + time(data.CreateTime) + "</td><td>" + data.Remake + "</td><td><a href='#' onclick='return Delete(" + data.ProId + ")'>删除</a><a href='/LiuJie/ChanPingYeMianUpdate?id=" + data.ProId + "'>修改</a></td></tr>");
                })
            },
            error: function (ex) {
                alert(ex.responseText);
            }
            });
    }

    //根据条件分页数据查询
    $("#sosuo").click(function () {
        TiaoJian();
    });

    //根据条件分页数据查询
    function TiaoJian() {
        var ProId = $("#id").val();
        $.ajax({
            type: "POST",
            url: "/LiuJie/GetTiaoJianPageListProbaict",
            data: "{pageIndex:" + pageIndex + ",pageSize:" + pageSize + ",ProId:'" + ProId+"'}",
            dataType: "json",
            contentType: "application/json",
            success: function (result) {
                pageCount = result.pageCount;
                $("#tbl tr").remove();
                $.each(result.DataList, function (index, data) {
                    $("#tbl").append("<tr><td><input  type='checkbox'name='subcheck'/></td><td>" + data.ProId + "</td><td>" + data.ProName + "</td><td>" + data.ProPrice + "</td><td>" + data.PorGuiGe + "</td><td>" + data.ProCId + "</td><td>" + data.UnId + "</td><td>" + time(data.CreateTime) + "</td><td>" + data.Remake + "</td><td><a href='#' onclick='return Del(" + data.ProId + ")'>删除</a><a href='/LiuJie/ChanPingYeMianUpdate?id=" + data.ProId + "'>修改</a></td></tr>");
                })
            },
            error: function (ex) {
                alert(ex.responseText);
            }
        });
    }

    //时间
    function time(m) {
        var dd = new Date(parseInt(m.substr(6)));
        return dd.toLocaleDateString();
    };

    //总条数
    function GetRows() {
        $.ajax({
            type: "POST",
            url: "/LiuJie/GetRowsProbaict",
            data: "{}",
            dataType: "json",
            contentType: "application/json",
            success: function (result) {
                //获取总条数
                rows = result;
                layui.use(['laypage', 'layer'], function () {
                    var laypage = layui.laypage
                        , layer = layui.layer;
                    //完整功能
                    laypage.render({
                        elem: 'DemoPageList'//分页容器的id
                        , count: rows//总条数
                        , limit: 3
                        , layout: ['count', 'prev', 'page', 'next', 'limit', 'refresh', 'skip']
                        , jump: function (obj, first) {
                            if (!first) {
                                //调用分页的方法
                                PageList(obj.curr, obj.limit);
                            }
                            console.log(obj)
                        }
                    });
                });
            },
            error: function (ex) {
                alert(ex.responseText);
            }
        });
    }

    //删除
    function Delete(id) {
        if (confirm('确认删除该产品吗？')) {
            $.ajax({
                type: "POST",
                url: "/LiuJie/GetDeleteProbaict",
                data: "{id:" + id + "}",
                dataType: "json",
                contentType: "application/json",
                success: function (result) {
                    if (result > 0) {
                        alert("删除成功!");
                        PageList();
                    } else {
                        alert("删除失败!");
                    }
                },
                error: function (ex) {
                    $("Error" + ex.responseText);
                }
            });
        }
        return false;
    }

    //复选框
    $("#subcheck").click(function () {
        var cf = $("#subcheck").prop("checked");
        $("[name=subcheck]").prop("checked", cf);
    });

    //批量删除
    $("#PiLiangShanChu").click(function () {
        if (confirm('确定删除多个产品吗')) {
            var count = 0;
            $("input[name='subcheck']:checked").each(function () {
                var n = $(this).parents("tr").index();//tr所在行
                var name = document.getElementById("tabInfo").getElementsByTagName("tr")[n + 1].getElementsByTagName("td")[1].innerHTML;//获取id
                alert(name);
                $.ajax({
                    type: "POST",
                    url: "/LiuJie/GetDeleteProbaict",
                    data: "{id:'" + name + "'}",
                    dataType: "json",
                    contentType: "application/json",
                    success: function (result) {
                        GetRows();
                    }, error: function (ex) {
                        alert("Error:" + ex.responseText);
                    }
                });
                count++;
            });
            if (count > 0) {
                alert("有" + count + "项数据删除成功！");
            } else {
                alert("删除失败！");
            }
        }
        return false;
    });
</script>
